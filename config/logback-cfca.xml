<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false" scan="true" nolog="true" scanPeriod="60 seconds">

    <property name="logging_home" value="logs/${application-name}"/>
    <property name="logging_backup_home" value="${logging_home}/backup"/>

    <!--设置日志输出为控制台-->
    <property name="CONSOLE_LOG_PATTERN"
              value="%red(%date{yyyy-MM-dd HH:mm:ss.SSS}) %highlight(%-5level) %red([%thread]) %boldMagenta(%logger{32}) %cyan(%msg%n)"/>

    <appender name="STDOUT" class="cfca.ch.qos.logback.core.ConsoleAppender">
        <!-- encoder class为空时, 默认也为 ch.qos.logback.classic.encoder.PatternLayoutEncoder -->
        <encoder class="cfca.ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="fileInfoLog" class="cfca.ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logging_home}/cfca-logging-info.log</file>
        <filter class="cfca.ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>DENY</onMatch>
            <onMismatch>ACCEPT</onMismatch>
        </filter>
        <rollingPolicy class="cfca.ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <FileNamePattern>
                ${logging_backup_home}/%d{yyyy-MM-dd}/cfca-logging-info.%d{yyyyMMdd}.%i.log.zip
            </FileNamePattern>
            <maxHistory>30</maxHistory>
            <maxFileSize>128MB</maxFileSize>
        </rollingPolicy>

        <layout class="cfca.ch.qos.logback.classic.PatternLayout">
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%-5level] [%logger:%line] - %msg%n
            </pattern>
        </layout>
    </appender>

    <appender name="fileErrorLog" class="cfca.ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logging_home}/cfca-logging-error.log</file>
        <filter class="cfca.ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <rollingPolicy class="cfca.ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <FileNamePattern>
                ${logging_backup_home}/%d{yyyy-MM-dd}/cfca-logging-error.%d{yyyyMMdd}.%i.log.zip
            </FileNamePattern>
            <maxHistory>30</maxHistory>
            <maxFileSize>128MB</maxFileSize>
        </rollingPolicy>

        <layout class="cfca.ch.qos.logback.classic.PatternLayout">
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%-5level] [%logger:%line] - %msg%n
            </pattern>
        </layout>
    </appender>

    <appender name="environmentInfoLog" class="cfca.ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logging_home}/cfca-logging-environment.log</file>

        <rollingPolicy class="cfca.ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <FileNamePattern>
                ${logging_backup_home}/%d{yyyy-MM-dd}/cfca-logging-environment.%d{yyyyMMdd}.%i.log.zip
            </FileNamePattern>
            <maxHistory>30</maxHistory>
            <maxFileSize>128MB</maxFileSize>
        </rollingPolicy>

        <layout class="cfca.ch.qos.logback.classic.PatternLayout">
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %level [%thread] - %msg%n
            </pattern>
        </layout>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="fileInfoLog"/>
        <appender-ref ref="fileErrorLog"/>
    </root>
    <logger name="environmentLogger" level="INFO">
        <appender-ref ref="environmentInfoLog"/>
    </logger>

</configuration>